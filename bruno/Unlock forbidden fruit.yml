info:
  name: Unlock forbidden fruit
  type: http
  seq: 2

http:
  method: GET
  url: https://flyt.vigoiks.no/{{env}}{{org}}/api/intern/authorization/me
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
