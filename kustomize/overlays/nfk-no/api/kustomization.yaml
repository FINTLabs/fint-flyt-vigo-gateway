apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: nfk-no

resources:
  - ../../../base

labels:
  - pairs:
      app.kubernetes.io/instance: fint-flyt-vigo-gateway_nfk_no
      fintlabs.no/org-id: nfk.no

patches:
  - patch: |-
      - op: replace
        path: "/spec/kafka/acls/0/topic"
        value: "nfk-no.flyt.*"
      - op: replace
        path: "/spec/orgId"
        value: "nfk.no"
      - op: add
        path: "/spec/url/basePath"
        value: "/nfk-no"
      - op: replace
        path: "/spec/ingress/basePath"
        value: "/nfk-no/api/vigo"
      - op: add
        path: "/spec/env/-"
        value:
          name: "logging.level.no.fintlabs.instance.gateway"
          value: "INFO"
      - op: replace
        path: "/spec/probes/readiness/path"
        value: "/nfk-no/actuator/health"
      - op: replace
        path: "/spec/observability/metrics/path"
        value: "/nfk-no/actuator/prometheus"
    target:
      kind: Application
      name: fint-flyt-vigo-gateway
